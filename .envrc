# direnv: activate the project virtualenv on `cd` into this directory.
# Makes bare `python`, `pytest`, and `streamlit` resolve to .venv/bin/* here,
# and restores your PATH when you leave. Run `direnv allow` once to trust it.
export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"
